public class LinearSearch {
    public LinearSearch(){}

    public int search(int item, int[] dataSet){
        int result = -1;

        for (int i = 0; i < dataSet.length; i++){
            if (dataSet[i] == item){
                result = i;
                break;
            }
        }

        return result;
    }
}
